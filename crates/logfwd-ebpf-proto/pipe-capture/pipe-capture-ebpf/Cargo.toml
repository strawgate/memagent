[package]
name = "pipe-capture-ebpf"
version = "0.1.0"
edition = "2021"

[dependencies]
aya-ebpf = "0.1"
pipe-capture-common = { path = "../pipe-capture-common" }

[[bin]]
name = "pipe-capture"
path = "src/main.rs"
