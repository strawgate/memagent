[package]
name = "pipe-capture"
version = "0.1.0"
edition = "2021"

[dependencies]
aya = "0.13"
pipe-capture-common = { path = "pipe-capture-common" }

[workspace]
members = ["pipe-capture-ebpf", "pipe-capture-common"]
