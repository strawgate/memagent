---
title: "Config Builder"
description: "Interactive pipeline config generator — pick an input, an output, and get a ready-to-run logfwd.yaml"
---

import ConfigBuilder from '../../../components/ConfigBuilder.astro';

Use the builder below to generate a `logfwd.yaml` for your pipeline.
Pick a preset to get started, or choose an input and output manually.
Optionally add a SQL transform to filter or reshape events before they reach the output.

<ConfigBuilder />

---

The generated YAML is validated against the same Rust config types used by `logfwd` itself,
so what you see here will work in production.
Fill in paths, endpoints, and credentials before deploying.
