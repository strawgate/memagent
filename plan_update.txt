1. Implement feedback from PR comments:
   - Add validation for `max_file_duration` to ensure it is > 0.
   - Wire `OutputType::Parquet` in `build_sink_factory`. Wait, we don't have a `ParquetSinkFactory` yet. Oh wait, do we? Let me check if there's a ParquetSinkFactory. If there is, I need to wire it. If not, maybe I'm supposed to add a dummy or maybe I missed it.
