pipelines:
  default:
    inputs:
      - type: file
        path: /var/log/app/*.log
        format: json
    outputs:
      - type: elasticsearch
        endpoint: http://localhost:9200
        index: logs
