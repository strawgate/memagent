\* TLC Model Checker Configuration — NIGHTLY THOROUGH safety model.
\*
\* Deeper than PipelineMachine.thorough.cfg: larger source set and batches to
\* exercise more permutations of concurrent multi-source behavior.  Runs under
\* the nightly schedule only (see .github/workflows/nightly-testing.yml).
\* PR-time CI runs no thorough configs — it sticks to safety, liveness, and
\* coverage models.  PipelineMachine.thorough.cfg is kept around as a
\* local-run convenience (`just tlc MCPipelineMachine.tla
\* PipelineMachine.thorough.cfg`) for mid-weight exploration between the
\* PR safety sizes and this nightly cfg.
\*
\* Run: java -XX:+UseParallelGC -cp tla2tools.jar tlc2.TLC MCPipelineMachine.tla \
\*          -config PipelineMachine.nightly.thorough.cfg -workers auto
\* Expected: billions of generated states; runs to completion in roughly
\* 60–120 minutes on the nightly runner (120-min timeout configured).
\*
\* NOTE: Do NOT use CONSTRAINT to bound state space — prefer model constants.
\* CONSTRAINT silently breaks liveness checking (not relevant here since this
\* is a safety config, but the rule applies workspace-wide).

SPECIFICATION Spec

CONSTANTS
    Sources = {"s1", "s2", "s3"}
    MaxBatchesPerSource = 4
    MaxNonTerminalHolds = 2

\* Safety invariants — strict superset of PipelineMachine.cfg's set, so
\* the deepest exploration validates everything the lighter PR-time
\* configs do (plus whatever new invariants get added to .cfg over time
\* should be mirrored here too).
INVARIANTS
    TypeOK
    StopMetadataConsistent
    NoDoubleComplete
    DrainCompleteness
    NoHeldWorkAfterStop
    QuiescenceHasNoSilentStrandedWork
    NoUnresolvedSentAtQuiescence
    CheckpointOrderingInvariant
    UnresolvedWorkNotCommittedPast
    CheckpointNeverAheadOfTerminalizedPrefix
    CommittedNeverAheadOfCreated
    SentImpliesCreated
    InFlightImpliesCreated
    InFlightImpliesSent
    HeldImpliesInFlight
    RetriedImpliesSent
    PanickedImpliesSent
    HoldCountOnlyForSent
    AckedImpliesCreated
    AckedImpliesSent
    RejectedImpliesCreated
    RejectedImpliesSent
    AbandonedImpliesCreated
    AbandonedImpliesSent

\* Temporal action properties — also a strict superset of PipelineMachine.cfg.
PROPERTIES
    NoCreateAfterDrain
    CommittedMonotonic
    HeldTransitionsDoNotCommit
    ForceStopAbandonsAllInFlight
    DrainMeansNoNewSending
    FailureTerminalizationPreservesCheckpoint
    FailureClassMustTerminalizePrototype
